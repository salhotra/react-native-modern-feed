{
  "name": "react-native-modern-feed",
  "version": "1.0.0",
  "description": "Elegant and simple list for beautiful content feed.",
  "main": "index.js",
  "scripts": {
    "build": "yarn lint && tsc --outDir lib",
    "format": "prettier --write './src/**/*.tsx'",
    "lint": "tslint -c tslint.json 'src/**/**'",
    "start": "concurrently \"tsc -w --outDir example/modernFeed\" \"yarn --cwd ./example start\"",
    "check:lint": "tsc --noEmit && yarn lint",
    "release": "yarn check:lint && yarn build && npm publish"
  },
  "keywords": [
    "react-native"
  ],
  "author": "Nishant Salhotra",
  "license": "MIT",
  "dependencies": {
    "@types/react": "^16.9.23",
    "@types/react-native": "^0.61.17",
    "prettier": "^1.19.1",
    "react": "^16.13.0",
    "react-native": "^0.61.5",
    "tslint": "^6.0.0",
    "tslint-config-prettier": "^1.18.0",
    "tslint-plugin-prettier": "^2.1.0",
    "typescript": "^3.8.2"
  },
  "peerDependencies": {
    "react": "^16.13.0",
    "react-native": "^0.61.5"
  }
}
